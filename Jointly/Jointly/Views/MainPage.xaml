<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:customui="clr-namespace:Jointly.CustomUI"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="Jointly.Views.MainPage"
             Visual="Material"
             x:Name="Page">
    <ContentPage.Resources>
        <Style x:Key="SetEvent" TargetType="ImageButton">
            <Setter Property="Source" Value="baseline_add_circle_black_36dp"/>
            <Setter Property="BackgroundColor" Value="Default"/>
            <Setter Property="WidthRequest" Value="50"/>
            <Setter Property="HeightRequest" Value="50"/>
            <Setter Property="VerticalOptions" Value="End"/>
            <Setter Property="HorizontalOptions" Value="Center"/>
            <Setter Property="Margin" Value="0, 0, 0, 5"/>
        </Style>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid>
            <maps:Map VerticalOptions="FillAndExpand"
                      HorizontalOptions="FillAndExpand"/>
            <ImageButton x:Name="SetEventButton"
                         Style="{StaticResource SetEvent}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>